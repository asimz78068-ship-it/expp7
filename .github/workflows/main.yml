name: Secret Demo

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Use Secret
        run: echo "Secret is used successfully"
        env:
          API_KEY: ${{ secrets.API_KEY }}
